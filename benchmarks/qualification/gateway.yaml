mode: gateway
prometheus:
  enabled: true
  address: :9090
shutdown_timeout: 10s
forwarding:
  routes:
  - store: mongo
    target: dns:///engine:8080
    tls:
      insecure: true
  dns_refresh_interval: 1s
grpc:
  max_send_message_bytes: 4MiB
